<!DOCTYPE html>
<html>
<head>
    <title>Rust Versions</title>
    <style>
        body {
            font-family: sans-serif;
            margin: 20px;
        }

        ul {
            list-style-type: none;
            padding: 0;
        }

        li {
            margin-bottom: 8px;
            padding: 8px;
            border-bottom: 1px solid #eee;
        }

        a {
            text-decoration: none;
            color: #007bff; /* Blue, for links */
        }

        a:hover {
            text-decoration: underline;
        }

        .version-info {
            font-weight: bold;
        }

        .version-date {
          font-style: italic;
          color: #666;
        }

        .latest-label{
          color: green;
        }
    </style>
</head>
<body>

    <h1>Rust Versions</h1>

    <ul>
        {% for version in versions %}
            <li>
                <span class="version-info"><a href="http://127.0.0.1:8080/info/{{ version.version }}">{{ version.version }}</a></span>
                <span class="version-date">({{ version.release_date }})</span>
                {% if version.latest_stable %} <span class="latest-label">(latest stable)</span>{% endif %}
                {% if version.latest_beta %} <span class="latest-label">(latest beta)</span>{% endif %}
                {% if version.latest_nightly %} <span class="latest-label">(latest nightly)</span>{% endif %}
            </li>
        {% endfor %}
    </ul>

</body>
</html>
